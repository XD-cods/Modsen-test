spring:
  application:
    name: library-service
  config:
    import: "configserver:"

springdoc:
  api-docs:
    path: /library-service/v3/api-docs
  swagger-ui:
    path: /library-service/swagger-ui.html