server:
  port: 0

spring:
  jpa:
    hibernate:
      ddl-auto: validate

openapi:
  service:
    title: library-service doc
    
springdoc:
  api-docs:
    path: /api/record/v3/api-docs
  swagger-ui:
    path: /api/record/swagger-ui.html