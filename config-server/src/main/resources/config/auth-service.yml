server:
  port: 0

spring:
  jpa:
    hibernate:
      ddl-auto: validate

openapi:
  service:
    title: auth-service doc
    
springdoc:
  api-docs:
    path: /api/auth/v3/api-docs
  swagger-ui:
    path: /api/auth/swagger-ui.html